# Cursor AI — secrets and local config
.cursor/mcp.json
.env
.env.local
.env*.local

# Cursor AI — keep in git (project knowledge)
# DO NOT ignore: .cursor/rules/, .cursor/skills/, .cursor/project-context.md

# Cursor Agent files (not for the public repo)
.agents/
skills-lock.json
# исходные видео с кириллическими именами — перемещены в assets/videos/
ведра.mp4
канистры.mp4
коробки.mp4

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*

# Audit outputs (local only)
audit-output/
reports/
*.audit.md
